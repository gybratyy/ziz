// types.ts

// Define the ProductItem interface (already provided)
export interface ProductItem {
    id: number;
    name: string;
    category: string;
    price: number;
    quantity: number;
    image: string;
}

